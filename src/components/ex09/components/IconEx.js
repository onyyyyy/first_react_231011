import { faUser } from "@fortawesome/free-solid-svg-icons";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

export const IconEx = () => {
  return (
    <div>
      <FontAwesomeIcon
        icon={faUser}
        style={{ color: "brown", fontSize: "26px" }}
      />
    </div>
  );
};
