231012 리액트 2일차
js를 불러오면 무조건 빈함수라도 만든다
=>컴포넌트(component)

1.js에 파일명 작성한거 함수로 사용할때 사용
컴포넌트로 사용할 함수는 대문자로 시작

2.반드시 특정값 반환(return)

3.반드시 태그 하나 작성

4.함수는 반드시 내보내줘야함 export default 함수명


npm install


jsx 문법 규칙
=>태그가 2개 이상일땐 반드시 부모 태그로 닫아줘야함

*컴포넌트
=> 레이아웃과 비슷하게 디자인의 하나의 영역을 뜻함
=> 컴포넌트 제작시 파일명과 함수명은 동일해야하며, 
첫글자는 대문자로 시작
=> 컴포넌트 함수를 제작 후 내보내기 export를 반드시 해줘야함
=>태그는 반드시 닫아줄것


*내보내기 불러오기
=>import 불러오기
=>export 내보내기

runtime error = 실행 후 에러

------------------------------------------
styled-component 사용시 
같은 태그에 다른 스타일 값을 주고싶을때에
(ex 배경색 같은)

props함수를 스타일 안에 사용할 수 없으니 
`background-color: ${(props) => props.bgColor};`

20231018
